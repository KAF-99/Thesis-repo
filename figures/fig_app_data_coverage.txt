Schematic data-availability timeline for the swap panel. Bars show nominal block coverage over the model sample; the dashed lines are the five walk-forward regime-fold boundaries (config \texttt{WF\_FOLDS}). The credit (IG/HY) block enters later than the curve/macro blocks. Exact per-column first/last-valid dates are not recomputed here because the licensed raw Bloomberg panel is git-ignored and absent from the repository; regenerate against \texttt{THESIS\_DATA\_PATH} for the precise ragged edges discussed in $\S$4.
