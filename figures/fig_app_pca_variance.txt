Realised cross-market PCA variance retained by horizon. The twelve-component cap (\texttt{XM\_PCA\_KMAX}$=12$) binds before the 95\% explained-variance target (\texttt{XM\_PCA\_VAR}$=0.95$): every fold selects $k=12$ components, retaining a mean of 0.738 of cross-market variance, below target. This is the honesty point of $\S$\,C — the cap, not the variance rule, determines the compression.
