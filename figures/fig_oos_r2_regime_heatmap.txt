Mean out-of-sample $R^2_{\text{OOS}}$ by model (rows) and regime (columns), pooled across horizons and tenors. Diverging shading centred at the random-walk benchmark ($0$). OLS is kept as a row but its cells are colour-clipped (its $-6$ to $-25$ values would otherwise wreck the scale); the annotation shows the true value. HTBoost lacks the Post-GFC cell at the one-year horizon (common-support note in the text).
