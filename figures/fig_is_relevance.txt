Feature-relevance share by taxonomy bucket for the three tree models (HTBoost relevance, XGBoost gain, CatBoost prediction-values-change), pooled across horizons, regimes and tenors and normalised within model. The models disagree on the leading bucket: HTBoost loads most on volatility, while XGBoost and CatBoost load most on the macro block (see Table~\ref{tab:is_relevance}).
